<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>ShopSmart</title>
<script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="p-5">
<h1 class="text-3xl font-bold mb-5">ShopSmart</h1>
<a href="/cart" class="bg-blue-500 text-white px-4 py-2 rounded mb-5 inline-block">View Cart</a>
<div class="grid grid-cols-3 gap-5">
  <% products.forEach(p => { %>
    <div class="border p-3 rounded">
      <h2 class="font-bold"><%= p.name %></h2>
      <p>Category: <%= p.category %></p>
      <p>Price: $<%= p.price %></p>
      <form action="/add-to-cart" method="POST">
        <input type="hidden" name="id" value="<%= p.id %>"/>
        <button class="bg-green-500 text-white px-3 py-1 mt-2 rounded">Add to Cart</button>
      </form>
    </div>
  <% }) %>
</div>
</body>
</html>
